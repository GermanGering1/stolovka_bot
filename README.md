# Telegram-бот для корпоративных обедов

Бот реализует выдачу одноразовых токенов для сотрудников, оформление заказов из подключенных ресторанов и автоматическую отправку отчётов менеджеру.

## Возможности
- Авторизация по Telegram ID и одноразовым паролям, выданным менеджером.
- Управление сотрудниками и лимитом дневных средств менеджером.
- Загрузка ресторанов и меню, выбор офиса с автоподстановкой ранее созданных значений.
- Заказ обедов на текущую неделю с учётом дедлайна 12:00 и лимита дневных средств (по умолчанию 400 ₽).
- Пополнение баланса при превышении лимита, уведомления о возврате лишних пополнений при изменении заказа.
- Запрет повторного заказа на выбранную дату, возможность изменять заказ до 12:00 дня выдачи.
- Ежедневная отправка XLS-отчётов по ресторанам в указанное время.

## Стек
- [Aiogram](https://docs.aiogram.dev/) для работы с Telegram Bot API.
- [SQLAlchemy](https://www.sqlalchemy.org/) и SQLite для хранения пользователей, меню и заказов.
- [APScheduler](https://apscheduler.readthedocs.io/) для плановой отправки отчётов.
- [pandas](https://pandas.pydata.org/) для генерации XLS-файлов.

## Настройка и запуск

### Запуск с Docker (рекомендуется)
1. Убедитесь, что установлены [Docker](https://docs.docker.com/get-docker/) и [Docker Compose](https://docs.docker.com/compose/install/).
2. Создайте файл `.env` в корне проекта:
   ```bash
   cp .env.example .env
   ```
3. Отредактируйте `.env`, указав ваш токен бота:
   ```env
   BOT_TOKEN=ваш_токен_бота_от_BotFather
   ADMIN_PASSWORD=admin123
   DEFAULT_LIMIT=400
   REPORT_HOUR=13
   REPORT_MINUTE=0
   ORDER_DEADLINE_HOUR=19
   TZ=Europe/Moscow
   ```
4. Запустите бота:
   ```bash
   docker-compose up -d
   ```
5. Просмотр логов:
   ```bash
   docker-compose logs -f
   ```

### Запуск без Docker
1. Создайте виртуальное окружение и установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
2. Определите переменные в `config.py` или через переменные окружения:
   - `BOT_TOKEN` — токен Telegram-бота.
   - `ADMIN_PASSWORD` — пароль менеджера (по умолчанию `admin`).
   - `REPORT_TIME` — время отправки отчётов (`datetime.time`).
   - `DB_NAME` и `DEFAULT_LIMIT` при необходимости.
3. Запустите бота:
   ```bash
   python main.py
   ```
   При первом запуске автоматически создаётся база данных и устанавливается базовый дневной лимит.

### Docker команды
- `docker-compose up -d` — запуск бота в фоновом режиме
- `docker-compose down` — остановка бота
- `docker-compose logs -f` — просмотр логов в реальном времени
- `docker-compose restart` — перезапуск бота
- `docker-compose exec telegram-bot bash` — вход в контейнер для отладки

## Структура проекта
```
├── main.py              # Точка входа
├── config.py            # Конфигурация бота
├── db.py                # Модели и подключение БД
├── requirements.txt     # Зависимости Python
├── Dockerfile          # Docker образ
├── docker-compose.yml  # Docker Compose конфигурация
├── .env.example        # Пример переменных окружения
├── handlers_auth.py    # Авторизация пользователей
├── handlers_manager.py # Панель менеджера
├── handlers_orders.py  # Заказы сотрудников
├── reports.py          # Генерация отчётов
├── states.py           # FSM состояния
├── data/               # База данных SQLite
└── logs/               # Логи приложения
```

## Основные файлы
- `main.py` — точка входа: инициализация БД, запуск диспетчера и планировщика отчётов.
- `handlers_auth.py` — авторизация и выбор интерфейса сотрудника/менеджера.
- `handlers_manager.py` — управление сотрудниками, меню и лимитом.
- `handlers_orders.py` — оформление и редактирование заказов сотрудниками.
- `reports.py` — генерация и отправка ежедневных XLS-отчётов по ресторанам.
- `states.py` — определения FSM-состояний для диалогов.

## Первые шаги
1. Менеджер входит по `ADMIN_PASSWORD` и попадает в панель управления.
2. Создайте сотрудников через кнопку «Управление сотрудниками» — бот выдаст одноразовый код приглашения.
3. Добавьте рестораны и блюда через «Меню/рестораны».
4. Сотрудники активируют бот по выданному коду, выбирают дату/ресторан и оформляют заказ.
5. В установленное время менеджер получает XLS-отчёты по каждому ресторану.

## Настройка переменных окружения
| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `BOT_TOKEN` | Токен Telegram бота (обязательно) | - |
| `ADMIN_PASSWORD` | Пароль для входа менеджера | `admin123` |
| `DEFAULT_LIMIT` | Дневной бюджет на сотрудника | `400` |
| `REPORT_HOUR` | Час отправки ежедневных отчётов | `13` |
| `REPORT_MINUTE` | Минута отправки ежедневных отчётов | `0` |
| `ORDER_DEADLINE_HOUR` | Дедлайн заказов на день | `19` |
| `TZ` | Часовой пояс сервера | `Europe/Moscow` |

## Решение проблем
1. **Бот не запускается**: проверьте наличие файла `.env` и корректность `BOT_TOKEN`.
2. **Ошибки подключения к БД**: убедитесь, что директория `data/` доступна для записи.
3. **Отчёты не отправляются**: проверьте настройки времени в `REPORT_HOUR` и `REPORT_MINUTE`.
4. **Ошибки в логах**: используйте `docker-compose logs -f` для просмотра детальной информации.

## Разработка и тестирование
Для локальной разработки можно использовать:
```bash
# Запуск без Docker
python main.py

# Или запуск с hot reload для разработки
pip install watchdog
watchmedo auto-restart --patterns="*.py" --recursive -- python main.py
```

## Лицензия
Проект распространяется под MIT License.